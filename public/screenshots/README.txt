Drop three PNG or JPG files here, named exactly:

  physics-sandbox.png
  pathfinder.png
  singularity.png

Cards render at 16:9, so capture around 1600x900. Until a file exists,
the card falls back to its generated artwork automatically.

Fastest way to capture:
  Windows  -> Win + Shift + S, drag over the canvas area
  Mac      -> Cmd + Shift + 4, drag
  Singularity already has a Screenshot button built into its controls.
